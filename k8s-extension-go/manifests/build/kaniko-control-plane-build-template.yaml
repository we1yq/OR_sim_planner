apiVersion: batch/v1
kind: Job
metadata:
  name: migrant-go-control-plane-build-NODE
  namespace: or-sim
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      nodeSelector:
        kubernetes.io/hostname: NODE
      hostNetwork: true
      dnsPolicy: Default
      tolerations:
        - operator: Exists
      containers:
        - name: kaniko
          image: gcr.io/kaniko-project/executor:debug
          imagePullPolicy: IfNotPresent
          command:
            - /busybox/sh
            - -c
            - |
              set -eu
              mkdir -p /workspace/context-control
              tar -xzf /input/context.tar.gz -C /workspace/context-control
              /kaniko/executor \
                --context=/workspace/context-control \
                --dockerfile=/workspace/context-control/Dockerfile.control-plane \
                --destination=localhost:10690/migrant-control-plane:go \
                --insecure \
                --skip-tls-verify
          volumeMounts:
            - name: build-context
              mountPath: /input
              readOnly: true
      volumes:
        - name: build-context
          configMap:
            name: migrant-go-build-context
