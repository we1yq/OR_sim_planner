apiVersion: mig.or-sim.io/v1alpha1
kind: ArrivalSnapshot
metadata:
  name: exp-resnet50-torchvision-20260602f
  namespace: or-sim
  labels:
    app.kubernetes.io/name: migrant-go
    experiment.or-sim.io/name: resnet50-torchvision-20260602f
spec:
  source: manual-experiment
  mode: target
  epoch: exp-resnet50-torchvision-20260602f
  triggerReason: real_torchvision_runtime_validation_with_versioned_image
  windowSeconds: 60
  unit: requestsPerSecond
  profileCatalogRef: default
  currentAllocationRef: physicalgpuregistries/default
  calibrationOverlayRef: physicalgpuregistries/default
  placement:
    nodes:
      - ampere
  registeredSLOMs:
    resnet50: 500
  targetArrival:
    resnet50: 0.08
  notes:
    - Validate real torchvision DEFAULT weights runtime with versioned model-runtime image.
    - Placement is restricted to ampere because rtx1 may be in use by other users.
