apiVersion: mig.or-sim.io/v1alpha1
kind: ArrivalSnapshot
metadata:
  name: exp-resnet50-torchvision-20260602h
  namespace: or-sim
  labels:
    app.kubernetes.io/name: migrant-go
    experiment.or-sim.io/name: resnet50-torchvision-20260602h
spec:
  source: manual-experiment
  mode: target
  epoch: exp-resnet50-torchvision-20260602h
  triggerReason: real_torchvision_runtime_validation_with_cached_weights
  windowSeconds: 60
  unit: requestsPerSecond
  profileCatalogRef: default
  currentAllocationRef: physicalgpuregistries/default
  calibrationOverlayRef: physicalgpuregistries/default
  placement:
    nodes:
      - ampere
  registeredSLOMs:
    resnet50: 500
  targetArrival:
    resnet50: 0.08
  notes:
    - Validate real torchvision DEFAULT weights runtime with pre-fetched image cache.
    - Placement is restricted to ampere because rtx1 may be in use by other users.
