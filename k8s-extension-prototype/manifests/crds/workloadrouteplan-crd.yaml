apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: workloadrouteplans.mig.or-sim.io
spec:
  group: mig.or-sim.io
  scope: Namespaced
  names:
    plural: workloadrouteplans
    singular: workloadrouteplan
    kind: WorkloadRoutePlan
    shortNames:
      - wrp
  versions:
    - name: v1alpha1
      served: true
      storage: true
      additionalPrinterColumns:
        - name: Action
          type: string
          jsonPath: .spec.action
        - name: Workload
          type: string
          jsonPath: .spec.workload
        - name: Preview
          type: boolean
          jsonPath: .spec.previewOnly
        - name: Phase
          type: string
          jsonPath: .status.phase
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                previewOnly:
                  type: boolean
                action:
                  type: string
                workload:
                  type: string
                sourceInstanceRef:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                queued:
                  type: integer
                target:
                  type: string
            status:
              type: object
              properties:
                phase:
                  type: string
                previewOnly:
                  type: boolean
                ownerActionPlan:
                  type: string
                validatedBy:
                  type: string
                message:
                  type: string
                observedGeneration:
                  type: integer
      subresources:
        status: {}
